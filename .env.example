# =============================================================================
# DEVEDOR360 v2 - CONFIGURACAO (TEMPLATE)
# =============================================================================
# Copie este arquivo para .env e preencha com seus valores reais:
#   cp .env.example .env
#
# ATENCAO: O arquivo .env contem tokens sensiveis. NAO commitar no git.

# --- AUTENTICACAO ---
# Tokens JWT separados por virgula (sem espacos entre tokens)
# Obtenha tokens em: https://sso.cloud.pje.jus.br/auth/realms/pje
PDPJ_TOKENS=seu_token_jwt_aqui

# --- API ---
PDPJ_BASE_URL=https://api-processo-integracao.data-lake.pdpj.jus.br/processo-api/api/v1/processos
PDPJ_TRIBUNAL=
PDPJ_ID_CLASSE=

# --- ENTRADA ---
# Nome do arquivo Excel com a lista de individuos/processos
INPUT_FILE=entrada.xlsx

# --- SAIDA ---
OUTPUT_DIR=outputs

# --- LIMITES DE BUSCA ---
MAX_POR_PAGINA=100
MAX_PAGINAS_POR_CASO=100
MAX_PROCESSOS_TOTAIS_POR_CASO=1000
MAX_PROCESSOS_ALERTA_API=5000
MAX_PROCESSOS_PER_DOC=1
MAX_PROCESSOS_PER_CNPJ_ROOT=2
MAX_FILIAIS=1

# --- CONTROLE DE EXECUCAO ---
DOWNLOAD_DETALHES=true
ENABLE_BUSCA_DOCUMENTO=true
ENABLE_BUSCA_NOME=true
ENABLE_BUSCA_FILIAL=true
ENABLE_BUSCA_NOME_RELACIONADO=false
WORKERS_PER_TOKEN=1
DEBUG=false

# --- FILTROS (vazio = filtro desativado) ---
FILTRO_MUNICIPIO=
BLACKLIST=

# --- CACHE ---
CACHE_DIR=.
SELIC_CACHE_FILE=selic_cache.json
